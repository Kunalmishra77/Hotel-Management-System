/**
 * Maintenance config — 11 (FR-4). One place for the preventive-reminder lead
 * time so it is a data edit, not a code hunt.
 */

/** How many days before a preventive job's due date the reminder fires (AC-6). */
export const MAINTENANCE_PREVENTIVE_REMINDER_LEAD_DAYS = 3;
