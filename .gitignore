# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env*.local
.env.local

# database
/prisma/*.db
/prisma/dev.db*

# testing
/coverage
/playwright-report
/test-results

# uploads (local dev object storage)
/public/uploads/*
!/public/uploads/.gitkeep

# misc
.DS_Store
*.pem
npm-debug.log*
.vscode/*
!.vscode/extensions.json
.idea

# database backups — encrypted dumps of production data; NEVER commit
/.backups
*.dump
*.dump.enc

# object storage (local sandbox for ID scans — encrypted, but never commit)
/.storage

# Build cache / session artifacts (not project source)
/tsconfig.tsbuildinfo
/.playwright-mcp/
/forbidden-403.png
/properties-overview.png

# Machine-local Claude Code settings
.claude/settings.local.json
.claude/*.tmp.*

# Generated by the 26 data-onboarding seed (sample import files)
/prisma/seed/fixtures/imports/
